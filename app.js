function openTool(toolName) {
  alert(
    toolName +
    "\n\nИнструмент скоро будет доступен. AI Factory уже запускается 🚀"
  );
}

document.addEventListener("DOMContentLoaded", () => {
  console.log("AI Factory запущен!");
});
